{
  "name": "vault-bug-report-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that files Vault bug reports as GitHub issues on behalf of the reporter.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
